apiVersion: v1
kind: Service
metadata:
  name: tweis-quantum-bank-backend-deployment-service
  namespace: tweis
  annotations:
    cloud.google.com/neg: '{"ingress":true}'
spec:
  type: LoadBalancer
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: tweis-quantum-bank-backend-deployment
